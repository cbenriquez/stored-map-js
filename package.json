{
  "name": "stored-map",
  "version": "1.0.4",
  "description": "An asynchronously iterable map object that holds key-value pairs stored as JSON files on the disk.",
  "keywords": ["store", "map", "datastore", "cache", "key-value"],
  "main": "dist/stored-map.js",
  "types": "dist/stored-map.d.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/cbenriquez/stored-map-js.git"
  },
  "scripts": {
    "test": "rm -r -f dist/* && tsc --build && node dist/test",
    "production": "rm -r -f dist/* && tsc --build"
  },
  "author": {
    "name": "Claude Bryan Enriquez",
    "email": "claudebryanenriquez@gmail.com"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.15",
    "@types/uuid-validate": "^0.0.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "copy-anything": "^3.0.2",
    "uuid-validate": "^0.0.3"
  }
}
